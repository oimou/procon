const N = 1e1;

console.log(N);

for (let i = 1; i <= N; i++) {
  process.stdout.write(`1 `);
}
process.stdout.write('\n');
for (let i = 1; i <= N; i++) {
  process.stdout.write(`1 `);
}
process.stdout.write('\n');
for (let i = 1; i <= N; i++) {
  process.stdout.write(`${i} `);
}
process.stdout.write('\n');
